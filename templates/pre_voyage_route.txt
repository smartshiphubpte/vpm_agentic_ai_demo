# Pre-voyage route plan
Voyage: {voyage_number}
Vessel: {vessel_id}
Route: {source_port} → {dest_port}
CP speed: {cp_speed_kn} kn
{cp_consumption_line}
Generated: {generated_at}
6-hour waypoints: {waypoint_count}

## Four optimized routes (sea only — land is a hard reject)
Fuel is omitted when CP consumption was not provided.
{alternatives_block}

## Alternate route waypoints (6-hour, all four)
{waypoint_table}

## Route map (master + four alternatives)
OSM.de basemap (same tiles as VoyagePM GUI). Master = blue; alternatives = coloured overlays with legend.
See map page in PDF.

## Waypoints (every 6 hours along master route)
{waypoint_block}

— PreVoyageIngestAgent / PreVoyageRouteOptimizeAgent
