# Python / tooling
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/

# VCS / IDE
.git/
.cursor/

# Secrets — never bake into the image (.env.example is kept)
.env
**/.env

# Local runtime outputs (compose mounts these; not part of the app)
data/
inbox/
noon_inbox/
storm_alerts/
reports/
weather_reports/
report_outbox/
port_weather_out/
temp.json
